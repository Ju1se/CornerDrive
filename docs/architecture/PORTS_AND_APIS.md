# Ports and APIs

The canonical API reference moved to [`../api/PORTS_AND_APIS.md`](../api/PORTS_AND_APIS.md).

This compatibility page stays in place so older links do not break, but new documentation should point to the `docs/api/` copy.
